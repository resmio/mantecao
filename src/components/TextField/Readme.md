Example:

    <TextField
      className='testClass'
      defaultValue='this is a TextField'
      hint='this is a hint'
      id='testId'
      label='this is a label'
      maxLength='22'
      minLength='3'
    />
